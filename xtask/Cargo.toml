[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bindgen = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile.workspace = true
